'use strict';

describe('myApp.materials module', function() {

  beforeEach(module('myApp.materials'));

  describe('materials controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var materialsCtrl = $controller('MaterialsCtrl');
      expect(materialsCtrl).toBeDefined();
    }));

  });
});