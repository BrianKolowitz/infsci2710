'use strict';

describe('myApp.project module', function() {

  beforeEach(module('myApp.project'));

  describe('project controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var projectCtrl = $controller('ProjectCtrl');
      expect(projectCtrl).toBeDefined();
    }));

  });
});